#!/bin/bash

# This script is meant to be the control center for data movement.
# We don't want to have to download the training/testing data onto our local machines, so we'll need some kind of script to import it into the working directory of the supercomputer.